export type FriendListType = {
  FriendName: string;
  Status: string;
};

export type RegisterFormData = {
  FirstName: string;
  Surname: string;
  Email: string;
  Password: string;
};

export type LoginFormData = {
  Email: string;
  Password: string;
};
