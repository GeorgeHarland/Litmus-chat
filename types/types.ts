export type FriendListType = {
  FriendName: string;
  Status: string;
};

export type LoginFormData = {
  Email: string;
  Password: string;
};

export type MessageType = {
  owner: string;
  message: string;
};
